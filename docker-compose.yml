services:
  ttlock-crm-adapter:
    build: .
    container_name: ttlock-crm-adapter
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "3052:3052"
    volumes:
      - ./data:/app/data
